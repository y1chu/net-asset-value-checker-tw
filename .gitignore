node_modules
.agents
skills-lock.json
.design\ui-redesign

# Netlify local state
.netlify/
*.log

# NOTE: data/fund-index.json is intentionally committed so search works on
# Netlify without a slow runtime crawl. `npm run build:index` refreshes it.
